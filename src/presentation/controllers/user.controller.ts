import { Controller } from '@nestjs/common';

// Note: UserController removed as User model no longer exists
// Member management will be handled in MemberController (to be created)

@Controller('user')
export class UserController {
  // User endpoints removed - User model no longer exists in schema
}
