// Note: UserRepositoryInterface removed as User model no longer exists
// This file kept for reference but interface is empty

export interface UserRepositoryInterface {
  // User model removed from schema - interface methods no longer needed
}
